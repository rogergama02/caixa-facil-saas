# --- Server ---
PORT=3000
JWT_SECRET=please_change_me

# --- Stripe (optional in dev) ---
# Create a product + recurring price in Stripe Dashboard, then copy the price ID (e.g., price_123...)
STRIPE_SECRET_KEY=
STRIPE_PRICE_ID=
# Webhook secret after running: stripe listen --forward-to localhost:3000/webhook
STRIPE_WEBHOOK_SECRET=
